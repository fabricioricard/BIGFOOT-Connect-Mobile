const config = {
  appId: 'com.bigfoot.network',
  appName: 'BIGFOOT Network',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  },
  android: {
    allowMixedContent: true
  }
};

module.exports = config;